> 🧬 **[EvoMate](https://github.com/rainnochen/EvoMate/blob/main/README.md)** — *Agent Digital Genetics Lab*
> [🏠 主页 / Home](https://github.com/rainnochen/EvoMate/blob/main/README.md) | [📖 最新 PRD](https://github.com/rainnochen/EvoMate/blob/main/docs-zh/prd-current.zh-CN.md) | [🏗 架构文档](https://github.com/rainnochen/EvoMate/blob/main/docs-zh/architecture.zh-CN.md) | [📝 开发日志](https://github.com/rainnochen/EvoMate/tree/main/开发日志) | [🤖 OpenClaw 接入](https://github.com/rainnochen/EvoMate/blob/main/docs-zh/openclaw-evomate-integration-prd.zh-CN.md)

---

# EvoMate 分布式开发机制

适用项目：`evolver-hackathon`

记录时间：2026-04-25 20:34:20

本文用于把 EvoMate Hackathon 项目拆成可以并行开发的模块，并约定每个模块如何通过稳定协议组装成一个可运行 Demo。

核心目标：

- 让多人可以同时开发，减少互相踩文件。
- 让每个模块有清晰输入、输出和验收标准。
- 让最后集成时只对齐协议，不靠临场猜测。
- 让 48 小时冲刺既快又稳。

## 1. 总原则

### 1.1 Contract First

所有模块先对齐协议，再写实现。

当前项目的核心协议是：

- `AgentGenome`：Agent 数字 DNA 数据结构。
- `CompatibilityResult`：父代匹配结果。
- `FusionResult`：子代生成结果。
- `ArenaOutput[]`：竞技场表达结果。
- `DemoStage`：前端展示阶段。

任何模块可以自由优化内部实现，但不能随意破坏这些协议。

### 1.2 单文件单主责

每个关键文件默认只有一个主责开发者。

如果确实需要多人改同一个文件，必须先在群里或现场同步：

```text
我准备改 apps/web/app.js 的 renderChildReport 部分，预计 20 分钟后交付。
```

### 1.3 Mock First, Adapter Later

Hackathon 阶段优先保证 Demo 稳定。

开发顺序是：

1. 先用 `data/mock/agents.json` 跑通。
2. 再用 adapter 接入 `openclaw-main` 或 `evolver-main`。
3. adapter 失败时，前端仍能回退到 mock 数据。

### 1.4 Integration Owner 合并

每轮集成必须有一个 Integration Owner。

职责：

- 拉齐模块协议。
- 合并各模块结果。
- 跑 `npm run check`。
- 启动本地 Demo。
- 记录开发日志。
- 决定是否进入路演版本。

### 1.5 Demo 不被实验拖垮

任何新功能都必须满足：

```text
失败时不影响当前主 Demo 闭环。
```

如果某个实验功能不稳定，默认隐藏到 feature flag、独立页面或文档中，不阻塞主流程。

## 2. 当前项目拆分为 7 个并行模块

### 模块 A：产品叙事与路演材料

适合角色：产品、路演、设计、会讲故事的人。

负责路径：

```text
docs-zh/
lightning-pitch/
README.md
开发日志/
```

主要任务：

- 维护一句话介绍。
- 维护 PRD、Demo 脚本和路演话术。
- 记录现场反馈。
- 生成和筛选 Pitch 图片。
- 把技术进展翻译成评委能理解的表达。

输出协议：

```text
PitchMessage
```

必须包含：

- `oneLiner`：一句话介绍。
- `demoHook`：30 秒内让人懂的钩子。
- `judgeKeywords`：贴合现场的关键词。
- `askForTeammates`：招募队友话术。

禁止事项：

- 不直接修改核心算法文件。
- 不把项目讲成纯娱乐宠物游戏。

### 模块 B：前端体验与视觉动效

适合角色：前端、UI、动效、视觉设计。

负责路径：

```text
apps/web/index.html
apps/web/styles.css
```

主要任务：

- 强化 Landing / Match Room / Fusion Chamber / Child Report / Arena 视觉。
- 优化移动端和投影屏展示。
- 做出“数字遗传实验室”的第一眼冲击力。
- 增强 DNA 融合、突变警报、Child Reveal 仪式感。

输入协议：

```text
DemoStage
AgentGenome
FusionResult
ArenaOutput[]
```

输出协议：

```text
DOM Slots
CSS Tokens
Animation Classes
```

约定：

- 不改 `src/core/` 算法。
- 如果需要新增 DOM 节点，必须和模块 C 对齐 id 或 class。
- 视觉状态通过 class 控制，例如 `is-running`、`child-output`、`empty-state`。

### 模块 C：前端编排与交互状态

适合角色：前端工程、集成工程。

负责路径：

```text
apps/web/app.js
```

主要任务：

- 加载数据。
- 管理父代选择状态。
- 调用 compatibility / fusion / arena 引擎。
- 把结果渲染到页面。
- 作为 UI 与核心逻辑之间的唯一编排层。

输入协议：

```js
calculateCompatibility(parentA, parentB)
fuseGenomes(parentA, parentB, compatibility)
generateArenaOutputs(task, parentA, parentB, child)
```

输出协议：

```text
Rendered Demo
DemoStage Event
```

约定：

- 不在 `app.js` 里写复杂遗传算法。
- 不在 `app.js` 里硬编码大量 Agent 数据。
- 新增交互时，先定义状态字段，再渲染。

### 模块 D：Genome Core 核心逻辑

适合角色：算法、Agent、后端、规则引擎。

负责路径：

```text
src/core/compatibility-engine.js
src/core/fusion-engine.js
src/core/arena-engine.js
src/README.md
```

主要任务：

- 优化匹配分数。
- 优化遗传、重组、突变机制。
- 输出更可信的继承报告。
- 支持稀有度、融合技能、多轮繁育。
- 让 Arena 输出更能体现父代和子代差异。

输入协议：

```text
AgentGenome
ArenaTask
```

输出协议：

```text
CompatibilityResult
FusionResult
ArenaOutput[]
```

约定：

- 纯函数优先。
- 不直接操作 DOM。
- 不直接读取文件。
- 不依赖浏览器全局变量。

### 模块 E：数据协议与 Agent Genome 内容

适合角色：数据、Prompt、Agent 设定、内容策划。

负责路径：

```text
data/mock/agents.json
module-manifest.json
```

主要任务：

- 扩展父代 Agent。
- 维护 AgentGenome 字段完整性。
- 设计更适合 Demo 的 Agent 组合。
- 维护 Arena 任务。
- 保证 mock 数据和 OpenClaw adapter 输出结构一致。

输出协议：

```text
GenomeBundle
```

约定：

- 每个 Agent 必须有 `id`、`name`、`archetype`、`rarity`、`avatar`。
- `soul` 必须是 `{ trait, weight }[]`。
- `skills`、`memory`、`knowledge` 必须是字符串数组。
- `radar` 必须包含 `logic`、`creativity`、`empathy`、`autonomy`、`risk`。
- `style` 必须是可读的表达风格描述。

### 模块 F：OpenClaw Gateway Adapter

适合角色：框架、Agent Runtime、集成工程。

负责路径：

```text
src/adapters/openclaw-gateway/
skill/
docs-zh/openclaw-evomate-integration-prd.zh-CN.md
```

主要任务：

- 维护 EvoMate 与真实 OpenClaw Gateway 的 WebSocket 连接（`openclaw-gateway-client.js`）。
- 把 OpenClaw Agent 数据映射为 AgentGenome（`openclaw-genome-extractor.js`）。
- 真实的技能执行指令下发与回复获取（`openclaw-skill-executor.js`）。
- 维护供 OpenClaw Agent 原生调用的进化技能（`skill/SKILL.md`）。
- 把 OpenClaw Agent workspace 映射为 AgentGenome。
- 把 `SOUL.md`、`IDENTITY.md`、skills、sessions、memory 映射为 DNA。
- 把 Evolver 的 mutation / memory / evaluation 能力映射到 EvoMate。
- 保证 adapter 输出和 `data/mock/agents.json` 结构一致。

输出协议：

```text
AgentGenome[]
```

约定：

- adapter 可以失败，但不能拖垮主 Demo。
- adapter 输出必须通过同一套 core engine。
- 前端不直接依赖 OpenClaw 或 Evolver，只依赖 AgentGenome。

### 模块 G：QA、发布与现场稳定性

适合角色：测试、集成、现场保障、工程 PM。

负责路径：

```text
scripts/
package.json
README.md
开发日志/
```

主要任务：

- 维护 `npm run check`。
- 维护本地启动说明。
- 做现场 Demo checklist。
- 记录每轮开发日志。
- 管控路演前最后稳定版本。

输出协议：

```text
ReleaseCandidate
```

必须包含：

- 可启动。
- 可点击完整链路。
- 可恢复到 mock 数据。
- 关键图片和文档路径可找到。
- 已记录本轮开发日志。

## 3. 组装协议

EvoMate 的组装方式分为 5 层。

### 3.1 数据组装协议：GenomeBundle

当前来源：

```text
data/mock/agents.json
```

结构：

```ts
type GenomeBundle = {
  arenaTask: string;
  agents: AgentGenome[];
};
```

```ts
type AgentGenome = {
  id: string;
  name: string;
  archetype: string;
  rarity: string;
  avatar: string;
  soul: Array<{ trait: string; weight: number }>;
  skills: string[];
  memory: string[];
  knowledge: string[];
  radar: {
    logic: number;
    creativity: number;
    empathy: number;
    autonomy: number;
    risk: number;
  };
  style: string;
};
```

规则：

- Mock、OpenClaw Adapter、Evolver Adapter 都必须输出 `AgentGenome[]`。
- UI 只认 `AgentGenome`，不关心数据来自哪里。
- 新字段可以增加，但不能删除现有字段。

### 3.2 核心逻辑协议：Core Engine API

当前核心 API：

```js
calculateCompatibility(parentA, parentB) => CompatibilityResult
fuseGenomes(parentA, parentB, compatibility) => FusionResult
generateArenaOutputs(task, parentA, parentB, child) => ArenaOutput[]
```

`CompatibilityResult`：

```ts
type CompatibilityResult = {
  total: number;
  personality: number;
  skill: number;
  knowledge: number;
  mutation: number;
  explanation: string;
};
```

`FusionResult`：

```ts
type FusionResult = {
  child: AgentGenome & {
    mutation: MutationResult;
  };
  inheritanceLog: string[];
  mutationLog: string[];
  fusionLog: string[];
};
```

`ArenaOutput`：

```ts
type ArenaOutput = {
  agent: string;
  role: 'Parent A' | 'Parent B' | 'Child';
  output: string;
  notes: string;
};
```

规则：

- Core Engine 不操作 DOM。
- Core Engine 不关心页面样式。
- Core Engine 可以被未来 CLI、OpenClaw plugin、测试脚本复用。

### 3.3 前端组装协议：DemoStage

当前 Demo 阶段：

```text
lab-entrance
match-room
fusion-chamber
child-report
arena
```

推荐事件名：

```text
evomate:parents-selected
evomate:compatibility-ready
evomate:fusion-started
evomate:child-created
evomate:arena-ready
```

事件 payload 建议：

```ts
type ParentsSelectedPayload = {
  parentAId: string;
  parentBId: string;
};
```

```ts
type ChildCreatedPayload = {
  parentAId: string;
  parentBId: string;
  childId: string;
  mutationType: string;
};
```

当前 MVP 可以不实现事件总线，但新增模块时应尽量按这些事件思考，避免在 UI 里互相强耦合。

### 3.4 Adapter 组装协议：Genome Source

所有外部来源都必须适配到统一结构：

```js
loadGenomes() => Promise<GenomeBundle>
```

建议或已有的路径：

```text
src/adapters/openclaw-gateway/openclaw-genome-extractor.js
```

adapter 规则：

- 成功时返回 `GenomeBundle`。
- 失败时抛出清晰错误。
- 前端捕获失败后回退到 mock 数据。
- adapter 不直接修改 UI。

### 3.5 发布组装协议：ReleaseCandidate

进入路演版本前必须满足：

```text
npm run check 通过
本地 Demo 可打开
至少一组父代可以成功 Breed
Child Report 有结果
Arena 有三列对比
开发日志已更新
路演话术已同步
```

## 4. 并行开发工作流

### 4.1 开工前 5 分钟

每个人认领一个模块，并说清楚：

```text
我负责模块 B，目标是强化 Fusion Chamber 动效，只改 apps/web/styles.css 和必要的 index.html。
```

### 4.2 开发中

规则：

- 只改自己模块负责路径。
- 如果需要跨模块，先同步。
- 每 30-60 分钟做一次小集成。
- 不把未完成的实验接到主 Demo 链路。

### 4.3 交付时

每个模块交付必须说明：

```text
改了什么
改了哪些文件
依赖什么协议
怎么验证
有什么风险
下一步建议
```

### 4.4 集成时

Integration Owner 执行：

```bash
npm run check
npm run dev
```

并手动验收：

```text
选择父代
查看匹配分数
点击 Breed
查看 Child Report
查看 Arena 对比
```

## 5. 文件所有权建议


| 文件或目录                              | 主责模块 | 合作模块  |
| ---------------------------------- | ---- | ----- |
| `apps/web/index.html`              | B    | C     |
| `apps/web/styles.css`              | B    | A     |
| `apps/web/app.js`                  | C    | B、D   |
| `src/core/compatibility-engine.js` | D    | E     |
| `src/core/fusion-engine.js`        | D    | E     |
| `src/core/arena-engine.js`         | D    | A、E   |
| `data/mock/agents.json`            | E    | A、D   |
| `docs-zh/`                         | A    | G     |
| `lightning-pitch/`                 | A    | B     |
| `scripts/`                         | G    | C     |
| `package.json`                     | G    | C     |
| `module-manifest.json`             | G    | A、D、E |
| `开发日志/`                            | G    | 全员    |


## 6. 冲刺任务拆分建议

### 任务 1：Fusion Chamber 视觉增强

模块：B

目标：

- 增加更明显的 DNA 双螺旋、粒子流、突变警报。
- Breed 后 1.4 秒内让观众感到“真的发生了融合”。

验收：

- 不改 core API。
- `npm run check` 通过。
- 投影屏上 5 米外能看懂正在融合。

### 任务 2：Child Reveal 仪式感

模块：B + C

目标：

- 子代诞生时增加视觉聚焦。
- 突出 Name、Rarity、Mutation Type。

验收：

- `FusionResult.child` 字段不变。
- Child Report 不空白。
- Arena 仍能正常生成。

### 任务 3：Genome Schema 强化

模块：D + E

目标：

- 增加 lineage、generation、source、visualGene 等字段。
- 兼容旧数据。

验收：

- 旧 `agents.json` 仍能运行。
- 新字段可以被 Child Report 展示。

### 任务 4：Family Tree 最小版

模块：B + C + D

目标：

- 展示 Parent A、Parent B、Child 的三节点家谱。
- 让“跨代演化”一眼可见。

验收：

- 不阻塞原有页面。
- 可以从 `FusionResult` 推导展示。

### 任务 5：OpenClaw Genome Adapter

模块：F

目标：

- 从 OpenClaw Agent 目录或配置中抽取一个最小 AgentGenome。
- 先做离线读取，不必接 Gateway。

验收：

- 输出结构能被 `calculateCompatibility` 消费。
- 失败时可回退 mock。

### 任务 6：路演版本收口

模块：A + G

目标：

- 把现场话术、Demo 顺序、图片资产、README 都对齐。
- 产出 2 分钟演示路径。

验收：

- 新队友 5 分钟内能读懂项目。
- 路演者 30 秒能说清楚项目。

## 7. 冲突处理规则

### 7.1 文件冲突

如果两个人都要改同一文件：

1. 先判断是否能拆到不同文件。
2. 不能拆时，由该文件主责人合并。
3. 合并后必须跑 `npm run check`。
4. 如果是 UI 文件，还要人工点击 Demo。

### 7.2 协议冲突

如果某模块想改 `AgentGenome`、`FusionResult` 等核心协议：

1. 先在文档中写清楚新增字段。
2. 只允许新增字段，不直接删除旧字段。
3. 修改 `module-manifest.json`。
4. 通知模块 C、D、E。
5. 更新开发日志。

### 7.3 Demo 风险

路演前 2 小时进入冻结期。

冻结期规则：

- 不做大重构。
- 不引入新依赖。
- 不改启动脚本。
- 不接不稳定外部服务。
- 只修 bug、调文案、调样式。

## 8. 日志与交接规则

每轮集成结束后，必须更新：

```text
开发日志/
```

参考：

```text
开发日志/开发日志规则.zh-CN.md
```

最低记录：

- 本轮目标。
- 实际完成。
- 修改文件。
- 验证结果。
- 遗留问题。
- 下一步建议。

## 9. 当前推荐团队配置

如果现场有 3 个人：

- 1 人负责 B + C：前端体验和编排。
- 1 人负责 D + E：Genome 逻辑和数据。
- 1 人负责 A + G：路演、文档、集成。

如果现场有 5 个人：

- 1 人负责 A：产品叙事和路演。
- 1 人负责 B：视觉动效。
- 1 人负责 C：前端编排。
- 1 人负责 D + E：核心逻辑和数据。
- 1 人负责 F + G：OpenClaw 接入和发布稳定性。

如果现场有 7 人：

- 每个模块 A-G 各 1 人。
- Integration Owner 从 G 中产生。

## 10. 当前最高优先级

从冲奖角度，优先级建议是：

1. 先保证主 Demo 完整稳定。
2. 再强化 Fusion 和 Child Reveal 的视觉冲击。
3. 再补 Family Tree，让跨代演化更直观。
4. 再接 OpenClaw Adapter，增强工程可信度。
5. 最后打磨路演话术和图片资产。

一句话：

```text
分布式开发不是每个人各写一坨，而是每个人围绕同一套 AgentGenome 协议，把不同层的进化体验拼成一个稳定 Demo。
```

